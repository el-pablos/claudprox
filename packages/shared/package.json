{
  "name": "@claudprox/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Tipe domain, konstanta, dan utilitas pemetaan model bersama ClaudProx",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
